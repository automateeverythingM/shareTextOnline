import { BypassStyleSanitizePipe } from './bypass-style-sanitize.pipe';

describe('BypassStyleSanitizePipe', () => {
  it('create an instance', () => {
    const pipe = new BypassStyleSanitizePipe();
    expect(pipe).toBeTruthy();
  });
});
